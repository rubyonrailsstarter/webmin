# Use the official Webmin Docker image as the base image
FROM webmin/webmin

# Optionally, you can add additional instructions or configurations here.
# For example, to expose additional ports or set environment variables.
# See Webmin documentation for customization options.
